schemaVersion: 2.2.0
metadata:
  name: my-demo-workspace
  version: 1.0.0
  description: Minimal Devfile to showcase key DevSpaces features
components:
  - name: dev-container
    container:
      image: quay.io/devspaces/udi-rhel9:latest
      memoryRequest: 256Mi
      memoryLimit: 8Gi
      cpuRequest: 100m
      cpuLimit: 2000m
      mountSources: true
      command: ["/bin/sh"]
      args: ["-c", "while sleep 1000; do :; done
  
